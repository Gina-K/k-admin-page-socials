// Mock API: json-server should run on localhost:3004.
// Mock API: json-server should run on localhost:3004. Assume user already logged-in and their id is user123.
export const BASE_URL = "http://localhost:3004";